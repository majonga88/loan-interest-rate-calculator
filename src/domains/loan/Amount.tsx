export class Amount {

    currentAmount: number;
    maxAmount: number;

    constructor(currentAmount: number, maxAmount: number) {
        this.currentAmount = currentAmount;
        this.maxAmount = maxAmount;
    }
}